alert("Bienvenido");

/*
			window.addEventListener("load", init);
			function init() {
				// body...
			var	circles = document.getElementByClassName('circle');
			for (i in circles){
				var circles = circles[i];
				var x = random(0,90);
				var y = random(0,90);
				if (typeof circle.style != "underfined"){
					circles.style.top = y + '%';
					circles.style.left = x + '%';
					circle.addEventListener('dragstart',dragIniciado,false);
					circle.addEventListener('dragend',dragFinalizado,false);
				}
			}
		}
		function dragIniciado(e){
			this.style.background =  blue ;
		}
		function dragFinalizado(e){
			this.style.background =  red ;
		}


		function random(min,max) {
			return Math.floor(Math.random() *(max -min + 1 )) + min;

		}

		
<script>
if (navegator.geolocation) {
	navegator.geolocation.getCurretPosition 
	(leerPosicion, error);
}
else {
	// Geolocalizacion no disponible, aplicar otra solucion
}

function leerPosicion (posicion) {
	latitud = posicion.coords.latitude;
	longitud = posicion.coords.longitude;
	//Enviar datos a API de Google Maps
}
	
</script>

		*/
